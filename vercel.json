{
  "version": 2,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/students.js" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
